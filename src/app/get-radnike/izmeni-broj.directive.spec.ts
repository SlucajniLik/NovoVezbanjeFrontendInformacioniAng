import { IzmeniBrojDirective } from './izmeni-broj.directive';

describe('IzmeniBrojDirective', () => {
  it('should create an instance', () => {
    const directive = new IzmeniBrojDirective();
    expect(directive).toBeTruthy();
  });
});
